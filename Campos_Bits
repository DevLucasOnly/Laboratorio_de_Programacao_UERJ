struct { // Estrutura anônima com campos de bit
    unsigned int e_pchave : 1; // Campo de 1 bit
    unsigned int e_externo : 1; // Campo de 1 bit
    unsigned int e_estatico : 1; // Campo de 1 bit
} flags;

// Uso:
flags.e_externo = 1; // Ativa o bit e_externo
if (flags.e_externo == 0 && flags.e_estatico == 0) // Testa os bits
    // ...
