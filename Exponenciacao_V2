/* pot: eleva a enésima potência; n >= 0; versão 2 */
int pot(int base, int n)
{
    int p;
    for (p = 1; n > 0; --n) { // Parâmetro n é modificado
        p = p * base;
    }
    return p;
}
